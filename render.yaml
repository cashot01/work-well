# render.yaml — WorkWell Global Solution 2025
services:
  # 🐘 Banco de dados PostgreSQL
  - type: postgres
    name: workwell-db
    databaseName: workwell_db
    user: workwell_user
    region: oregon

  # 🐇 RabbitMQ via CloudAMQP (add-on)
  - type: addon
    addon: cloudamqp
    name: workwell-rabbitmq
    plan: lemur
    region: oregon

  # 🚀 Aplicação Spring Boot (Docker)
  - type: web
    name: workwell-app
    runtime: docker
    region: oregon
    envVars:
      # 🔐 Banco de dados (Render injeta DATABASE_URL automaticamente)
      - key: SPRING_DATASOURCE_URL
        value: ${DATABASE_URL}
        # Spring Boot 3.1+ suporta postgres:// diretamente!
        # Se quiser forçar JDBC, use: jdbc:postgresql://${DATABASE_URL#postgres://}
      - key: SPRING_DATASOURCE_USERNAME
        fromDatabase:
          name: workwell-db
          property: user
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: workwell-db
          property: password

      # 🐇 RabbitMQ (CloudAMQP injeta CLOUDAMQP_URL)
      - key: SPRING_RABBITMQ_HOST
        fromAddon:
          name: workwell-rabbitmq
          property: host
      - key: SPRING_RABBITMQ_PORT
        fromAddon:
          name: workwell-rabbitmq
          property: port
      - key: SPRING_RABBITMQ_USERNAME
        fromAddon:
          name: workwell-rabbitmq
          property: username
      - key: SPRING_RABBITMQ_PASSWORD
        fromAddon:
          name: workwell-rabbitmq
          property: password
      - key: SPRING_RABBITMQ_VIRTUAL_HOST
        fromAddon:
          name: workwell-rabbitmq
          property: virtualHost
      - key: SPRING_RABBITMQ_SSL_ENABLED
        value: "true"

      # 🤖 Hugging Face (adicione sua chave no dashboard)
      - key: HUGGINGFACE_API_KEY
        sync: false  # você define no dashboard (não commita!)

      # 📦 Hibernate/Flyway
      - key: SPRING_JPA_HIBERNATE_DDL_AUTO
        value: "none"
      - key: SPRING_FLYWAY_ENABLED
        value: "true"
      - key: SPRING_FLYWAY_BASELINE_ON_MIGRATE
        value: "true"

      # 🌐 Porta (necessária para Spring Boot no Render)
      - key: SERVER_PORT
        value: "8080"

    healthCheckPath: /actuator/health
    healthCheckInterval: 30